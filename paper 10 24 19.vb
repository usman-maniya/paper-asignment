Module Module1

    Sub Main()
        Dim s1 As Strings
        Dim validbinarystring As Boolean
        Dim stringsize As Integer
        Dim stringchar As Char
        Dim denary As Integer

        validbinarystring = True

        Print(" Enter binary number ")
        Input(s1)

       If 
            If stringsize < 7 Or stringsize > 0 Then
                validbinarystring = False
            End If

        If stringchar ≠ 0 or stringchar ≠ 1 Then
                validbinarystring = False
            End If

            then

            Print(" binary number is invalid ")
        Else

            s1 = denary

        End If

        Print(denary value is "denary")
    End Sub

End Module
